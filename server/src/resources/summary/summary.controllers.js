export const getSummary = (req, res) => {
  res.json({ message: "getSummary" });
};

export const deleteSummary = (req, res) => {
  res.json({ message: "deleteSummary" });
};

export const updateSummary = (req, res) => {
  res.json({ message: "updateSummary" });
};
